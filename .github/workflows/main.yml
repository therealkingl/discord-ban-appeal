name: Windows - RustDesk

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    timeout-minutes: 9999
    
    steps:
      - name: Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://www.dwservice.net/download/dwagent_x86.exe" -OutFile "dwagent.exe"
                  dwagent.exe -silent key=465-509-379

